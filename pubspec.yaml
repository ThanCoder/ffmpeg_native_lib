name: ffmpeg_native_lib
description: "native libs for ffi project."
version: 0.0.1
repository: https://github.com/ThanCoder/ffmpeg_native_lib.git

environment:
  sdk: ^3.12.0

dependencies:
  code_assets: ^1.2.1
  hooks: ^2.0.2
  logging: ^1.3.0
  path: ^1.9.1
  ffi: ^2.1.4
  archive: ^4.0.9

dev_dependencies:
  ffigen: ^20.1.1
  flutter_lints: ^6.0.0
  test: ^1.28.0
